# 🔐 Tournament Management System - 환경변수 설정 예시
# 이 파일을 .env로 복사한 후 실제 값으로 변경하세요.

# ===== 서버 설정 =====
# 실행 환경 (development, production, test)
NODE_ENV=development

# 서버 포트 번호
PORT=5000

# ===== 데이터베이스 설정 =====
# SQLite (로컬 개발용)
DATABASE_URL="file:./prisma/dev.db"

# PostgreSQL (프로덕션용 예시)
# DATABASE_URL="postgresql://username:password@localhost:5432/tournament_db"

# ===== JWT 보안 설정 =====
# ⚠️ 중요: 32자 이상의 강력한 랜덤 문자열을 사용하세요
# 생성 방법: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long
JWT_REFRESH_SECRET=your-super-secret-refresh-key-different-from-jwt-secret

# JWT 토큰 만료시간 (1시간 액세스, 7일 리프레시)
JWT_ACCESS_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# ===== 관리자 계정 설정 =====
# 기본 관리자 계정 (시스템 초기화 시 생성됨)
ADMIN_EMAIL=admin@tournament.com
ADMIN_PASSWORD=admin123!@#secure

# ===== 클라이언트 설정 =====
# 프론트엔드 URL (CORS 허용용)
CLIENT_URL=http://localhost:3000

# ===== 보안 설정 =====
# bcrypt 해시 라운드 수 (높을수록 안전하지만 느림)
BCRYPT_SALT_ROUNDS=10

# 세션 시크릿 (선택사항, JWT_SECRET과 다르게 설정 권장)
SESSION_SECRET=your-session-secret-key-here

# ===== 외부 서비스 API 키 =====
# OpenAI API (AI 스케줄링 기능용, 선택사항)
OPENAI_API_KEY=sk-your-openai-api-key-here

# ===== 이메일 서비스 설정 (선택사항) =====
# SMTP 서버 설정 (이메일 알림 기능용)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password

# ===== 파일 업로드 설정 =====
# 최대 파일 크기
MAX_FILE_SIZE=10MB

# 업로드 디렉토리
UPLOAD_DIR=uploads

# ===== 개발용 추가 설정 =====
# 디버그 모드 활성화
DEBUG=tournament:*

# 로그 레벨 (error, warn, info, debug)
LOG_LEVEL=info

# ===== 프로덕션 배포 설정 예시 =====
# 프로덕션 환경에서는 다음과 같이 설정하세요:

# NODE_ENV=production
# DATABASE_URL="postgresql://prod_user:secure_password@your-db-host:5432/tournament_prod_db"
# JWT_SECRET="production-grade-super-secure-secret-key-minimum-32-characters"
# JWT_REFRESH_SECRET="different-production-refresh-secret-key-minimum-32-characters"
# CLIENT_URL="https://your-frontend-domain.com"
# ADMIN_PASSWORD="super-secure-admin-password-for-production"

# ===== 보안 주의사항 =====
# 1. 이 파일(.env.example)은 Git에 커밋되지만, .env 파일은 커밋하지 마세요
# 2. 프로덕션에서는 반드시 강력한 비밀번호와 키를 사용하세요
# 3. JWT_SECRET과 JWT_REFRESH_SECRET은 서로 다른 값을 사용하세요
# 4. 정기적으로 시크릿 키를 업데이트하세요
# 5. 환경변수는 배포 플랫폼의 환경설정에서 관리하세요